#include "main.h"

/**
 * main - check the code for
 *
 * Return: Always 0.
 */
int main(void)
{
    int array2[1] = { 98 };

    print_array(array2, 0);
    print_array(array2, -1);
    return (0);
}
