#include "main.h"

/**
 * _abs - program for alphabet printing
 *
 * Description: 'print alphabet in lowercase'
 *
 * @a: entry
 *
 * Return: 1 if lowercase, 0 in other cases
 */

int _abs(int a)
{
	if (a < 0)
	{
		a = -a;
	}
	return (a);
}
