#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'description for betty requirement'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n = 0;

	char _a = '\n';

	while (n < 10)
	{
		printf("%d", n);
		n++;
	}
	putchar(_a);
	return (0);
}
