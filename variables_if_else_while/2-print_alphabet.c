#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'description for betty requirement'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char n = 'a';
	char _a = '\n';

	while (n <= 'z')
	{
	putchar(n);
	putchar(_a);
	n++;
	}
	return (0);
}
